# backend/

Python FastAPI application (auth, RBAC, validation, business logic, database
access, communication with `ai_service/`). Built starting Phase 4 (FastAPI
foundation) and extended through Phase 5 onward (authentication, Admin core,
Coach core, Player portal APIs, reports).

Empty in Phase 1.
