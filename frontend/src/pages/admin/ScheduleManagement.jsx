// Admin Schedule Management renders the ONE shared Schedule Management page
// (pages/shared/ScheduleManagement.jsx) — the exact same component Coach
// Schedule Management uses. Do not fork this file; edit the shared
// component so Admin and Coach stay pixel-identical.
export { default } from '../shared/ScheduleManagement.jsx';
