# ml/

The ML notebook (`CricTalentAI_ML.ipynb`), dataset audit, target definition,
feature engineering, model training and evaluation (Random Forest, XGBoost,
K-Means). Works from the historical/sample dataset — `players.xlsx` and
`match_performance.xlsx`, currently in the project root — plus
`training_records.xlsx` once it is generated.

Populated from Phase 11 (Dataset / ML Preparation) onward.

Empty in Phase 1.
